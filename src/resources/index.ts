import { i18nContent } from "./content-i18n";

const defaultContent = i18nContent.en;

export const {
  person,
  social,
  newsletter,
  home,
  about,
  blog,
  work,
  gallery,
  chat,
} = defaultContent;

export {
  display,
  mailchimp,
  routes,
  protectedRoutes,
  baseURL,
  fonts,
  style,
  schema,
  sameAs,
  socialSharing,
  effects,
  dataStyle,
} from "./once-ui.config";
